{
  type Direction = 'left' | 'right' | 'up' | 'down';

  let move: Direction = 'right';
  // move = 'hello';

  // function first<T>(array: T[]): T {
  //   if(array[0]) return array[0];
  // }

  // const v1 = first([1, 'd', true, '3', 4]);
}