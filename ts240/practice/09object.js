let obj1 = {
  oldProp: 'old',
  newProp: 'new',
};

// let obj2 = Object.create(obj1, {newProp: 'really new'}); 
// properties object (i.e., the 2nd argument) MUST be a properties "map" not a common object with properties


console.log(obj2);