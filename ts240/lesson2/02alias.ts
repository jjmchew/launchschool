{
  // yes - an error will be raised on the definition of `person2`
  //        - name must be a string, and age must be a number


  // given
  // type Name = string;
  // type Age = number;
  // type Person = {
  //   name: Name;
  //   age: Age;
  // };

  // function greet(person: Person): string {
  //   return `Hello, ${person.name}! You are ${person.age} years old.`;
  // }

  // const person1: Person = {
  //   name: "Alice",
  //   age: 30,
  // };

  // const person2: Person = {
  //   name: 42,
  //   age: "Bob",
  // };

  // console.log(greet(person1));
  // console.log(greet(person2));
}